services:
  - type: web
    name: hate-speech-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python api/app.py
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: 5000

